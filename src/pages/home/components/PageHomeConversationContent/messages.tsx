import { Flex } from "@mantine/core";
import clsx from "clsx";

export default function PageHomeConversationContentMessages() {
  return (
    <Flex direction={"column"} className={clsx(`flex-1`)} bg={"gray.2"}>
      {/* TODO: add messages information */}
    </Flex>
  );
}
